export enum Roles {
  USER = 'USER',
  DOCTOR = 'DOCTOR',
  MANAGER = 'MANAGER',
}

export enum TypeOfPatient {
  ADULT = 'ADULT',
  CHILD = 'CHILD',
}

export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}
